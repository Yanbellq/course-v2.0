# Аналіз курсового проекту: Інформаційна система комп'ютерної фірми

## 📋 Тема курсової
**Варіант 27. Інформаційна система комп'ютерної фірми**

## ✅ Що вже реалізовано

### 1. Моделі даних (apps/crm/models.py, apps/main/models.py)

#### ✅ Наявні моделі:
- ✅ **Supplier** - постачальники
- ✅ **Contract** - договори з постачальниками
- ✅ **Supply** - постачання товарів
- ✅ **Product** - товари/комплектуючі
- ✅ **ProductCategory** - категорії товарів
- ✅ **User** - користувачі/клієнти
- ✅ **Employee** - працівники
- ✅ **Sale** - продажі
- ✅ **Repair** - ремонти (з підтримкою гарантійних)
- ✅ **CustomBuild** - кастомні збірки ПК
- ✅ **SoftwareService** - послуги налаштування ПЗ
- ✅ **Delivery** - доставки

### 2. Базові CRUD операції
- ✅ CRUD для постачальників
- ✅ CRUD для договорів
- ✅ CRUD для постачань
- ✅ CRUD для працівників
- ✅ CRUD для продажів
- ✅ CRUD для ремонтів
- ✅ CRUD для товарів

### 3. Автентифікація та авторизація
- ✅ JWT автентифікація
- ✅ Ролі користувачів (user, operator, admin)
- ✅ Middleware для безпеки

### 4. Аналітика (частково)
- ✅ Базовий analytics_api endpoint
- ✅ Підрахунок виторгу за місяці
- ✅ Статистика ремонтів

---

## ❌ Що відсутнє або потребує доопрацювання

### 🔴 КРИТИЧНО: 10 запитів з теми НЕ РЕАЛІЗОВАНІ

Згідно з темою курсової, потрібно реалізувати **10 конкретних запитів**:

#### ❌ Запит 1: Постачальники та продукція
**Вимога:** Вивести інформацію про постачальників, а також продукцію, яку вони постачають; знайти комплектуючі, які постачаються двома і більше постачальниками.

**Статус:** ❌ НЕ РЕАЛІЗОВАНО
- Потрібен endpoint: `/api/queries/suppliers-with-products/`
- Потрібен endpoint: `/api/queries/components-multiple-suppliers/`

#### ❌ Запит 2: Клієнти, які придбали продукцію
**Вимога:** Вивести повну інформацію про клієнтів, які придбали комп'ютерну техніку (комплектуючі) вказаної фірми.

**Статус:** ❌ НЕ РЕАЛІЗОВАНО
- Потрібен endpoint: `/api/queries/customers-by-manufacturer/?manufacturer=Intel`

#### ❌ Запит 3: Ремонти за гарантією
**Вимога:** Вивести повну інформацію про товари, які були повернуті в ремонт за гарантією; інформацію про товари, які було відремонтовано за гарантією; інформацію про всі ремонтні роботи, які виконуються за гарантією.

**Статус:** ⚠️ ЧАСТКОВО (є модель Repair з repair_type='warranty', але немає окремих endpoints)
- Потрібен endpoint: `/api/queries/warranty-repairs-returned/`
- Потрібен endpoint: `/api/queries/warranty-repairs-completed/`
- Потрібен endpoint: `/api/queries/warranty-repairs-in-progress/`

#### ❌ Запит 4: Виторг та продажі
**Вимога:** Визначити максимальну суму виторгу за визначений місяць (від поточної дати); за різними видами товару за останній тиждень (від поточної дати); вивести інформацію про те, скільки продукції було продано кожним продавцем.

**Статус:** ⚠️ ЧАСТКОВО (є analytics_api, але не повністю відповідає вимогам)
- Потрібен endpoint: `/api/queries/max-revenue-by-month/?month=2025-01`
- Потрібен endpoint: `/api/queries/revenue-by-product-type-last-week/`
- Потрібен endpoint: `/api/queries/sales-by-employee/`

#### ❌ Запит 5: Комплектуючі за ціною
**Вимога:** Отримати інформацію про перелік необхідних комплектуючих, вартість яких не перевищує заданої суми.

**Статус:** ❌ НЕ РЕАЛІЗОВАНО
- Потрібен endpoint: `/api/queries/components-by-price/?max_price=5000`

#### ❌ Запит 6: Постачальники з найбільшою кількістю ремонтів
**Вимога:** Отримати інформацію про постачальників, товари яких найчастіше потрапляють в ремонт.

**Статус:** ❌ НЕ РЕАЛІЗОВАНО
- Потрібен endpoint: `/api/queries/suppliers-by-repair-frequency/`

#### ❌ Запит 7: Комплектуючі кастомної збірки
**Вимога:** Отримати інформацію про те, з яких комплектуючих складається комп'ютер, зібраний на замовлення для деякого клієнта.

**Статус:** ⚠️ ЧАСТКОВО (є модель CustomBuild, але немає endpoint)
- Потрібен endpoint: `/api/queries/custom-build-components/?user_id=xxx` або `/api/queries/custom-build-components/?sale_id=xxx`

#### ❌ Запит 8: Товари від постачальників за період
**Вимога:** Отримати інформацію про те, скільки вказаного товару було одержано від постачальників за визначений період.

**Статус:** ❌ НЕ РЕАЛІЗОВАНО
- Потрібен endpoint: `/api/queries/product-supplies-by-period/?product_id=xxx&start_date=2025-01-01&end_date=2025-12-31`

#### ❌ Запит 9: Договори за період
**Вимога:** Отримати повну інформацію про договори, які були укладні за визначений період; підрахувати кількість договорів, укладених з кожним постачальником.

**Статус:** ❌ НЕ РЕАЛІЗОВАНО
- Потрібен endpoint: `/api/queries/contracts-by-period/?start_date=2025-01-01&end_date=2025-12-31`
- Потрібен endpoint: `/api/queries/contracts-count-by-supplier/`

#### ❌ Запит 10: Постачальник за номером договору
**Вимога:** Отримати повну інформацію про постачальника товару за номером договору.

**Статус:** ❌ НЕ РЕАЛІЗОВАНО
- Потрібен endpoint: `/api/queries/supplier-by-contract-number/?contract_number=12345678`

---

## ⚠️ Інші проблеми та недоліки

### 🔴 КРИТИЧНО: Відсутність екрану маніпулювання даними через SQL запити/агрегації
**Згідно з методичкою (стор. 5, 178, 215-217):**
- ❌ **ОБОВ'ЯЗКОВО** потрібен екран маніпулювання даними за допомогою SQL запитів / агрегацій
- ❌ Оператор СД має мати можливість виконувати вбудовані SQL-запити або агрегації
- ❌ Оператор СД має мати можливість формувати нові SQL-запити / агрегації
- ❌ Авторизований користувач має мати можливість виконувати вбудовані SQL-запити / агрегації та зберігати результати

**Статус:** ❌ НЕ РЕАЛІЗОВАНО
- Потрібен екран/endpoint для виконання SQL-запитів (для реляційних БД) або агрегацій (для MongoDB)
- Потрібен екран/endpoint для формування нових запитів
- Потрібен механізм збереження результатів запитів

### 1. Відсутність Customer моделі
- ❌ Використовується `User` замість окремої моделі `Customer`
- ⚠️ Можливо, це прийнятно, але потрібно перевірити вимоги методички

### 2. Відсутність зв'язку між Product та Supplier
- ❌ Немає прямого зв'язку Product → Supplier
- ⚠️ Зв'язок існує через Contract → Supply → Product, але це ускладнює запити

### 3. Відсутність поля manufacturer у зв'язку з Sale
- ⚠️ У Sale немає прямого посилання на manufacturer товару
- Потрібно робити join через products → product_id → manufacturer

### 4. Відсутність документації (згідно з методичкою)
**Згідно з методичкою (розділ 3):**
- ❌ **User Stories** - відсутні
- ❌ **Use Case діаграма** - відсутня
- ❌ **Use Case сценарії** - відсутні
- ❌ **Документування класів** - відсутнє
- ❌ **Документування модулів** - відсутнє
- ❌ **Статична структура програмної системи (UML-діаграма класів)** - відсутня
- ❌ **Огляд GUI** - відсутній (потрібні скріншоти)
- ❌ **Тестування** - відсутнє (потрібні тести користувацького доступу та CRUD)
- ❌ **Розгортання** - відсутнє (потрібен опис процедури розгортання)

### 5. Тестування
**Згідно з методичкою (підрозділ 3.4):**
- ❌ **Тестування користувацького доступу (UAT)** - відсутнє
- ❌ **Тестування операцій з даними (CRUD)** - відсутнє
- ❌ Потрібні скріншоти з GUI для демонстрації тестування

### 6. Валідація даних
- ⚠️ Часткова валідація в serializers
- ⚠️ Потрібно додати більше перевірок

### 7. Відсутність ER-діаграми
**Згідно з методичкою (підрозділ 2.3):**
- ❌ Потрібна ER-діаграма структури сховища даних
- ❌ Потрібен опис нормалізації до 3 нормальної форми

### 8. Права доступу
**Згідно з методичкою (стор. 219-253):**
- ⚠️ Потрібно перевірити, чи правильно реалізовані права:
  - Адміністратор СД: додавання операторів, реєстрація користувачів після заявок
  - Оператор СД: формування нових SQL-запитів/агрегацій
  - Авторизований: збереження результатів вбудованих SQL-запитів/агрегацій
  - Гість: відправка заявки на реєстрацію

---

## 📝 Рекомендації щодо виправлення

### Пріоритет 1 (КРИТИЧНО): Реалізувати 10 запитів з теми

1. **Створити новий файл** `apps/api/views/queries_views.py`
2. **Додати URLs** в `apps/api/urls/` (створити `queries_urls.py`)
3. **Реалізувати кожен запит** з використанням MongoDB aggregation pipeline або QuerySet

### Пріоритет 1.5 (КРИТИЧНО): Екран маніпулювання даними через SQL/агрегації

**Згідно з методичкою (стор. 5, 178, 215-217, 233-235, 245-247) це ОБОВ'ЯЗКОВА вимога!**

1. **Створити екран/endpoint** для виконання MongoDB aggregation queries
2. **Реалізувати вбудовані запити** (10 запитів з теми)
3. **Реалізувати можливість формування нових запитів** (для операторів)
4. **Реалізувати збереження результатів** (для авторизованих користувачів)
5. **Додати GUI екран** для маніпулювання даними (якщо веб-інтерфейс)

### Пріоритет 2: Покращити структуру даних

1. **Додати поле `supplier_id` до Product** (опціонально, для спрощення запитів)
2. **Додати поле `manufacturer` до Sale.products** (для швидшого пошуку)

### Пріоритет 2: Додати документацію (згідно з методичкою)

**Згідно з методичкою (розділ 3) потрібно:**

1. **User Stories** - описати вимоги у форматі "ЯК <роль>, Я ХОЧУ <мета>, ЩОБ <мотив>"
2. **Use Case діаграма** - створити діаграму варіантів використання
3. **Use Case сценарії** - описати детальні сценарії взаємодії
4. **Документування класів** - описати всі класи (назва, опис, атрибути, методи, залежності)
5. **Документування модулів** - описати модулі (функціональність, структура, API, використання)
6. **UML-діаграма класів** - статична структура програмної системи
7. **Огляд GUI** - скріншоти всіх екранів
8. **Опис розгортання** - процедура інсталяції та вимоги до апаратного/програмного забезпечення

### Пріоритет 3: Тестування (згідно з методичкою)

**Згідно з методичкою (підрозділ 3.4) потрібно:**

1. **Тестування користувацького доступу (UAT)**:
   - Тест авторизації користувача (з скріншотами)
   - Тест доступу оператора системи (з скріншотами)
   
2. **Тестування операцій з даними (CRUD)**:
   - Тест на додавання нових даних (з скріншотами)
   - Тест на редагування даних (з скріншотами)
   - Тест на видалення даних (з скріншотами)
   - Тест на перегляд даних (з скріншотами)

### Пріоритет 4: ER-діаграма та нормалізація

**Згідно з методичкою (розділ 2):**

1. **ER-діаграма** - створити діаграму структури сховища даних
2. **Опис нормалізації** - показати нормалізацію до 3 нормальної форми

---

## 🔧 Конкретні кроки для виправлення

### Крок 1: Створити структуру для запитів

```python
# apps/api/views/queries_views.py
from rest_framework.decorators import api_view, permission_classes
from rest_framework.permissions import IsAuthenticated
from rest_framework.response import Response
from apps.crm.models import Supplier, Contract, Supply, Sale, Repair, Employee
from apps.main.models import Product, User
from datetime import datetime, timedelta
```

### Крок 2: Реалізувати кожен запит

Приклад для Запиту 1:
```python
@api_view(['GET'])
@permission_classes([IsAuthenticated])
def query1_suppliers_with_products(request):
    """Запит 1: Постачальники та продукція"""
    suppliers = Supplier.objects().all()
    result = []
    for supplier in suppliers:
        # Знайти всі договори з цим постачальником
        contracts = Contract.objects().filter(supplier_id=supplier._id).all()
        products = []
        for contract in contracts:
            for product_item in contract.products:
                product = Product.find_by_id(product_item.product_id)
                if product:
                    products.append({
                        'product_id': str(product._id),
                        'product_name': product.name,
                        'quantity': product_item.quantity,
                        'unit_price': product_item.unit_price
                    })
        result.append({
            'supplier': {
                'id': str(supplier._id),
                'name': supplier.name,
                'contact_person': supplier.contact_person,
                'email': supplier.email,
                'phone': supplier.phone
            },
            'products': products
        })
    return Response(result)
```

### Крок 3: Додати URLs

```python
# apps/api/urls/queries_urls.py
from django.urls import path
from apps.api.views.queries_views import *

app_name = 'queries'

urlpatterns = [
    path('suppliers-with-products/', query1_suppliers_with_products, name='query1'),
    path('components-multiple-suppliers/', query1_components_multiple_suppliers, name='query1b'),
    # ... інші запити
]
```

---

## 📊 Підсумок

### ✅ Сильні сторони:
- Добре структурований проект
- Використання MongoDB ORM
- Базові CRUD операції реалізовані
- Автентифікація та безпека налаштовані

### ❌ Слабкі сторони:
- **КРИТИЧНО:** Відсутні всі 10 запитів з теми
- Відсутня документація
- Відсутні тести
- Деякі зв'язки між моделями можна покращити

### 🎯 Головна проблема:
**Всі 10 запитів з теми курсової НЕ РЕАЛІЗОВАНІ.** Це критично, оскільки саме ці запити є основною вимогою курсової роботи.

---

## ⏱️ Оцінка часу на виправлення

- Реалізація 10 запитів: **8-12 годин**
- Екран маніпулювання даними через SQL/агрегації: **6-8 годин** ⚠️ КРИТИЧНО
- Покращення структури даних: **2-3 години**
- Додавання документації (User Stories, Use Cases, UML): **8-12 годин** ⚠️ ОБОВ'ЯЗКОВО
- Написання тестів з скріншотами: **6-8 годин** ⚠️ ОБОВ'ЯЗКОВО
- ER-діаграма та опис нормалізації: **3-4 години** ⚠️ ОБОВ'ЯЗКОВО

**Загалом: 33-47 годин роботи**

---

## 📋 ДОДАТКОВІ ВИМОГИ З МЕТОДИЧКИ

### Обов'язкові екрани (згідно з методичкою, стор. 174-178):
1. ✅ Екран ідентифікації користувача - **РЕАЛІЗОВАНО** (JWT автентифікація)
2. ✅ Екран додавання користувачів - **РЕАЛІЗОВАНО** (CRUD для User)
3. ✅ Екран(-и) введення/виведення інформації з СД - **РЕАЛІЗОВАНО** (CRUD для всіх сутностей)
4. ❌ **Екран маніпулювання даними за допомогою SQL запитів / агрегацій** - **НЕ РЕАЛІЗОВАНО** ⚠️ КРИТИЧНО

### Права доступу (згідно з методичкою, стор. 219-253):
- ✅ Адміністратор СД - частково реалізовано (role='admin')
- ⚠️ Оператор СД - частково реалізовано (role='operator'), але потрібна можливість формування нових запитів
- ⚠️ Авторизований - частково реалізовано (role='user'), але потрібна можливість збереження результатів запитів
- ❌ Гість - не реалізовано (потрібна можливість відправки заявки на реєстрацію)

---

*Дата аналізу: 2025-01-27*
*Версія проекту: 2.0*

